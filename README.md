# my-aap

## Google Maps street lookup

If you want street autocomplete and geocoding through Google Maps, set:

```bash
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_key
```

Without this variable, the app falls back to OpenStreetMap street lookup.
